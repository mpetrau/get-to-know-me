<h1>Offers#index</h1>
<p>Find me in app/views/offers/index.html.erb</p>
<% @offers.each do |offer| %>
  <%= link_to offer.deal.name, offer_path(offer) %>
  <%= render partial: 'show', locals: { offer: offer }%>
<% end %>
